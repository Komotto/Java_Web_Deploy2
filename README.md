# Java_Web_Deploy
Aplicação web simples em **Java puro**, sem Maven, servindo "Olá Mundo" via `HttpServer`.

## Estrutura
- `src/Main.java` → código fonte
- `out/` → classes compiladas
- `run.sh` → script para compilar e executar

## Como rodar

### 1. Compilar
```bash
bash run.sh compile



